node_modules/
.DS_Store
.claude/settings.local.json

# Raw, unoptimized source material — one folder per project. Only the processed
# /images/*-optimized/ folders are actually used by the site and committed.
images/atari-2600/
images/canal-district/
images/coffee-guy/
images/high-seas-brewing/
images/jayl-house/
images/project-search/
images/soo-falls/
images/soo-falls-selects/

# Exports/alternates not referenced by any page (kept on disk, not tracked)
images/unused/

# Local working notes / session checkpoints — useful for picking a session
# back up, not meant to be read by anyone else or tracked in history.
/notes/
